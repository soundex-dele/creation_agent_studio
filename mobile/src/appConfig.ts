export const APP_CONFIG = Object.freeze({
  /** Additional origins allowed to use the optional native bridge. */
  trustedAuthOrigins: [] as readonly string[],
  customUrlScheme: 'agentstudio',
  userAgentSuffix: 'AgentStudioMobile/1.0',
});
