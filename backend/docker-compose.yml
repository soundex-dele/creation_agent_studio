services:
  web:
    build: .
    restart: unless-stopped
    command: sh -c "python manage.py migrate --noinput && daphne -b 0.0.0.0 -p 8080 backend.asgi:application"
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: backend.settings.production
      DB_HOST: postgres
      REDIS_HOST: redis
      REQUIRED_EXECUTION_WORKER_POOLS: agent,media,workflow
      REQUIRE_AUTOMATION_SCHEDULER: "true"
      ARTIFACT_ROOT: /data/artifacts
      AGENT_WORKSPACE_ROOT: /data/agent-workspaces
      MEDIA_ROOT: /data/media
      APPLICATION_RUNTIME_ALLOWED_ROOTS: /data
    ports:
      - "8080:8080"
    volumes:
      - runtime_data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/healthz/"]
      interval: 15s
      timeout: 5s
      retries: 5

  agent-worker:
    build: .
    restart: unless-stopped
    command: python manage.py run_execution_coordinator --worker-pool agent
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: backend.settings.production
      DB_HOST: postgres
      REDIS_HOST: redis
      ARTIFACT_ROOT: /data/artifacts
      AGENT_WORKSPACE_ROOT: /data/agent-workspaces
      MEDIA_ROOT: /data/media
      APPLICATION_RUNTIME_ALLOWED_ROOTS: /data
    volumes:
      - runtime_data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "manage.py", "check_execution_heartbeat", "--worker-pool", "agent"]
      interval: 15s
      timeout: 5s
      retries: 5

  media-worker:
    build: .
    restart: unless-stopped
    command: python manage.py run_execution_coordinator --worker-pool media
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: backend.settings.production
      DB_HOST: postgres
      REDIS_HOST: redis
      ARTIFACT_ROOT: /data/artifacts
      AGENT_WORKSPACE_ROOT: /data/agent-workspaces
      MEDIA_ROOT: /data/media
      APPLICATION_RUNTIME_ALLOWED_ROOTS: /data
    volumes:
      - runtime_data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "manage.py", "check_execution_heartbeat", "--worker-pool", "media"]
      interval: 15s
      timeout: 5s
      retries: 5

  workflow-worker:
    build: .
    restart: unless-stopped
    command: python manage.py run_execution_coordinator --worker-pool workflow
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: backend.settings.production
      DB_HOST: postgres
      REDIS_HOST: redis
      ARTIFACT_ROOT: /data/artifacts
      AGENT_WORKSPACE_ROOT: /data/agent-workspaces
      MEDIA_ROOT: /data/media
      APPLICATION_RUNTIME_ALLOWED_ROOTS: /data
    volumes:
      - runtime_data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "manage.py", "check_execution_heartbeat", "--worker-pool", "workflow"]
      interval: 15s
      timeout: 5s
      retries: 5

  scheduler:
    build: .
    restart: unless-stopped
    command: python manage.py run_automation_scheduler
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: backend.settings.production
      DB_HOST: postgres
      REDIS_HOST: redis
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "manage.py", "check_execution_heartbeat", "--scheduler"]
      interval: 30s
      timeout: 5s
      retries: 5

  maintenance:
    build: .
    restart: unless-stopped
    command: python manage.py enforce_retention --loop --poll-interval 3600
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: backend.settings.production
      DB_HOST: postgres
      REDIS_HOST: redis
      ARTIFACT_ROOT: /data/artifacts
      AGENT_WORKSPACE_ROOT: /data/agent-workspaces
      MEDIA_ROOT: /data/media
    volumes:
      - runtime_data:/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "manage.py", "check_execution_heartbeat", "--maintenance"]
      interval: 60s
      timeout: 5s
      retries: 5

  postgres:
    image: postgres:14
    restart: unless-stopped
    container_name: creation_studio_postgres
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: ${DB_ADMIN_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_ADMIN_PASSWORD:?DB_ADMIN_PASSWORD must be configured}
      APP_DB_NAME: ${DB_NAME:?DB_NAME must be configured}
      APP_DB_USER: ${DB_USER:?DB_USER must be configured}
      APP_DB_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD must be configured}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init-app-db.sh:/docker-entrypoint-initdb.d/10-app-db.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7
    restart: unless-stopped
    container_name: creation_studio_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    build: ../frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      web:
        condition: service_healthy

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  runtime_data:
    driver: local
