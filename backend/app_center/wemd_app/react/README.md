# WeMD web renderer

The platform builds the upstream WeMD web source as the isolated `wemd.html`
entry declared in `frontend/vite.config.ts`. This directory marks the
platform-owned renderer boundary required by the application package manifest.
