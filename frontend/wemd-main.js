// Keep the standalone WeMD document rooted in the host Vite project so the
// same entry works in both the dev server and the multi-page production build.
import '../backend/app_center/WeMD/apps/web/src/main.tsx'
